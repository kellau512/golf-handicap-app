{
  "name": "golf-handicap-app",
  "version": "0.1.0",
  "private": true,
  "description": "Local golf handicap calculator with snapshotted course lookup.",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "test": "node --test",
    "refresh:courses": "node server.js --refresh-course-index",
    "warm:details": "node server.js --warm-course-details"
  },
  "engines": {
    "node": ">=18"
  }
}
